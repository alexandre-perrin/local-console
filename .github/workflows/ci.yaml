---
name: CI

on:
  pull_request:
  workflow_dispatch:

jobs:
  check:
    name: formatting
    runs-on: [self-hosted, X64]
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Run Pre-commit
      uses: pre-commit/action@v3.0.0
